<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.tacocloud.mapper.TacoOrderTacosMapper">
    <!--    public Account getAcct(Long id); -->
    <insert id = "saveTacoOrder">
        insert into taco_order_tacos(`taco_order`,`taco`) values (#{tacoOrderId}, #{tacoId})
    </insert>
</mapper>